import 'package:flutter/material.dart';

class UserErrorModel extends StatelessWidget {
  const UserErrorModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
